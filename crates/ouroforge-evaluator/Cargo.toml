[package]
name = "ouroforge-evaluator"
version = "0.1.0"
edition = "2021"

[dependencies]
ouroforge-evidence = { path = "../ouroforge-evidence" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
