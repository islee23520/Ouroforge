# Behavior evidence bundle v1 fixture

Fixture-scoped behavior lifecycle evidence for #621 GL10.11.1.

The bundle is inert audit data. It links structured behavior definitions, runtime events, scenario outcomes, drafts, review decisions, apply transactions, rollback metadata, and rerun comparisons without adding script execution, command bridges, browser trusted writes, or auto-apply behavior.
