# GDD Prototype Evidence Bundle fixtures

Fixture-scoped examples for Issue #657. These JSON artifacts are committed only as deterministic validation fixtures; generated run/evidence output remains ignored unless explicitly fixture-scoped.
