# Signal Gate Platformer

This fixture is the Milestone 21 second game class example. It models a one-screen platformer/obstacle/signal path through the same Seed -> Build -> Observe -> Verify -> Journal -> Evolve loop shape used by collect-and-exit.

Committed evidence is deterministic and fixture-scoped. Generated run outputs belong under `runs/` and remain untracked.
