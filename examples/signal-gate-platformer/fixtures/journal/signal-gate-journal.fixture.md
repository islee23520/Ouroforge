# Signal Gate Journal Fixture

Run: `m21-signal-gate-loop-demo`

The second game class reused the existing loop shape without introducing a per-game evaluator bypass. Source fixtures, four-gate verdict evidence, loop coverage, and comparison evidence are all fixture-scoped.

Structured gap evidence is recorded in `examples/signal-gate-platformer/fixtures/generalization/gap-found.fixture.json` for generalization work that remains descriptive rather than silently patched.
