# Source Patch Apply Transaction v1 fixture

`apply-transaction.sample.json` is an inert source-patch apply transaction
artifact. It links preview, sandbox, review, file-class, diff-integrity,
rollback, verification, and post-apply evidence refs without granting apply,
merge, browser write, or command execution authority.
